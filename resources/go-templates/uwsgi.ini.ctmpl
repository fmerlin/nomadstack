[uwsgi]
die-on-idle = false
idle = 1800
chdir = /app/
wsgi-file = $(WSGI_FILE)
socket = {{ env "NOMAD_ADDR_uwsgi" }}
protocol = uwsgi
disable-logging = true
processes = 4
threads = 4
master = true
module = app
callable = app
